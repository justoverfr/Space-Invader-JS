import { shipPosX, shipPosY } from "./ship";

var posXmissile = shipPosX
var posYmissile = shipPosY
var domMissile = document.getElementsByClassName("laser")

function tireMissile(){
    var key = event.keyCode
}